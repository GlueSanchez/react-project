const defaultState = {};

export const customerReducer = (state = defaultState, action) => {
    switch (action.type) {
        // case
        default:
            return state;
    }
};

// export const doAction = (dos)=> ({type: TypeError,  dos}) ;