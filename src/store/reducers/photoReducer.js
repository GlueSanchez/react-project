const defaultState = {};

export const photosReducer = (state = defaultState, action) => {
    switch (action.type) {
        // case
        default:
            return state;
    }
};

// export const doAction = (dos)=> ({type: TypeError,  dos}) ;